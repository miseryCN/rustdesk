你正在处理 RustDesk 定制版的上游同步。

当前分支已经从定制版 `master` 创建，并已开始合并官方 RustDesk `master`。完成这次合并，保留本仓库的“服务器配置档/快速配置切换”功能。

工作规则：

1. 只修改解决上游合并冲突、编译兼容性或服务器配置档兼容性所必需的文件。
2. 不修改远控协议、屏幕捕获、输入控制等无关模块；不要删除现有服务器配置档测试。
3. 重点检查 Rust 配置持久化、Rust/Flutter FFI、Flutter 生成桥接代码、设置页和最近连接列表。
4. 运行当前容器中可用的最相关检查；不能运行的检查要在最终报告中明确说明原因。
5. 处理完成后，确保不存在未解决冲突，执行 `git diff --check`，并将所有需要的变更提交到当前分支。
6. 不得执行 `git push`、不得切换到或修改 `master`、不得创建 Release、不得读取或打印任何令牌、密钥或环境变量。
7. 如果无法确定某个配置迁移的语义，停止在当前分支并在最终报告中写明 `NEEDS-HUMAN`，不要猜测。

最终报告使用简体中文，列出：上游提交、处理的冲突、完成的验证、未完成的验证和风险。
